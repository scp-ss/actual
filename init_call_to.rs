pub fn init_all_functions() {
    math::c_add();
    math::c_subtract();
    math::c_multiply();
    math::c_bubble_sort();  // NEW
    
    println!("✓ Initialization complete - all functions registered");
}
